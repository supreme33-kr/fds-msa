apiVersion: v1
kind: ConfigMap
metadata:
  name: alertmanager-config
  namespace: fds
data:
  alertmanager.yml: |
    route:
      receiver: 'default'
      group_wait: 10s
      group_interval: 30s
      repeat_interval: 1h
    receivers:
    - name: 'default'
