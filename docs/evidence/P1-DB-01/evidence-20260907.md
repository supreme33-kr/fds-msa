# P1-DB-01 Evidence — 2026-09-07

## 개요
- Exit Criteria: transaction-api 승인 Source 접속·저장·조회 및 접근차단 Evidence 확보
- 검증 환경: edge01 k3s (fds namespace), db01 (10.1.93.55:5432/fdsdb)
- 검증자: 이재환

## 핵심 요구사항 검증

### 1. Credential 하드코딩 금지 — PASS
- transaction-api/app/db.py: DATABASE_URL = os.environ["DATABASE_URL"] — 환경변수에서만 읽음, 코드에 실제 값 없음
- alembic/env.py의 <pw>/*** 표기는 주석의 예시 표기일 뿐 실제 값 아님
- kubernetes/base/secret.example.yaml은 템플릿 파일, 값은 CHANGE_ME placeholder
- git log --all --full-history -- "**/secret.yaml" → 실제 secret.yaml 커밋 이력 없음 확인
- .gitignore에 kubernetes/base/secret.yaml 명시적 제외 확인

### 2. transaction-api 승인 경로만 허용 — PASS
- pg_hba.conf 허용 목록(hostssl, fdsdb, fds_app): 10.1.93.105/32, 10.1.93.107/32, 10.1.93.108/32, 10.1.93.50/32 (Worker 3대 + edge01)

### 3. Client/fds-engine 직접 DB 접근 차단 — PASS
- kubectl get deployment fds-engine -n fds -o yaml에 env 섹션 자체가 없음 → fds-engine은 DATABASE_URL을 포함한 어떠한 DB 자격증명도 보유하지 않음
- transaction-api만 DB에 직접 접근하는 구조 확인

### 4. 시간대/Timestamp 저장 기준 고정 — PASS
- occurred_at은 UTC(tzinfo=utc)로 저장
- R04(심야고액) 판정 시점에만 KST(Asia/Seoul) 변환 적용 (09-03 팀 결정 반영, fds-engine/app/rules.py)

## Acceptance Criteria 검증

### 1. Schema/Migration이 재현 가능하다 — PASS
- Alembic 기반 정식 버전관리 (P1-DB-03, 09-03 완료)
- alembic stamp 0001로 기존 데이터 보존하며 채택, alembic current = "0001 (head)" 확인
- 커밋: bbd78f4 (fds-msa)

### 2. transaction-api 저장·조회가 성공한다 — PASS
- 저장: transaction-api-7445cc89d5-ffjfj pod에서 POST http://localhost:8000/api/v1/transactions 호출
  - body: {"account_id":"evidence-acct-1788751869","amount":1500000,"currency":"KRW","transaction_type":"deposit","source_ip":"10.1.93.1"}
  - 응답: 201, transaction_id: 9f2c82a1-45e5-44e6-bccf-5268f0fc8c8d, fds_detected: false, fds_rules: R01/R02/R04/R07 모두 triggered:false
- 조회: 동일 account_id로 DB 직접 조회(asyncpg) → row 정상 반환
  - fds_rules 컬럼(JSON)에 룰 평가 결과 저장 확인 (09-03 로그의 "triggered_rules 미저장" 이슈는 컬럼명이 fds_rules였던 것으로 확인, 해소됨)

### 3. 미승인 Source 접속이 거부된다 — PASS
- db01에서 자기 자신의 실제 IP(10.1.93.55)로 hostssl 접속 시도
- 결과: "SSL 암호화 연결에 대한 설정이 pg_hba.conf 파일에 없음" 에러로 정상 차단 확인
- 참고: db01 자기 자신은 pg_hba.conf 허용 목록에 미등록 — P1-DB-05(pg_hba.conf 최종 축소) 시 의도된 설계인지 확인 필요 (이권욱 확인 대기)

### 4. 기본 Backup 명령과 보관경로가 검증된다 — PASS (선행 실증 참조)
- 2026-08-27 저녁 세션에서 실증 완료: pg_dump -F c 백업 → TRUNCATE로 장애 재현 → pg_restore로 복구 → 신규 거래 E2E 재검증까지 Exit Criteria 전 과정 통과 (P1-DR-01 사전 실증, 별도 정식 검증은 P1-DR-01 티켓에서 진행 예정 09-10~09-13)
- 백업 파일/테스트 데이터는 db01에 Evidence로 보존 중

## 남은 확인 사항 (이번 PR 범위 아님)
- db01 자기 참조가 pg_hba.conf에 없는 것이 의도된 설계인지 확인 필요 (@GWONUK 확인 요청)
- fail_open 컬럼 존재 여부 미확인

## 결론
핵심 요구사항 4종, Acceptance Criteria 4종 전부 Evidence 확보 완료. P1-DB-01 Done 전환 가능.
